---
description: Generate side banner (310×600) + header (1366×200) for a new EzGo Hospitality venue. Usage — /banner-create [business_name] [url]
argument-hint: [business_name] [url]
allowed-tools: Task, Bash, Read, Write, Edit, Glob, Grep, AskUserQuestion
---

Generate the EzGo banner + header for the venue.

**Inputs:**
- `business_name`: $1
- `url`: $2 (optional — treat as empty if missing)

**Action — YOU (the main agent) run the orchestration DIRECTLY on the main thread. Do NOT dispatch `banner-orchestrator` as a sub-agent.** A background sub-agent cannot open `AskUserQuestion` gates to the user and cannot nest the worker sub-agents (verified 2026-07-01 — the dispatched orchestrator stalled after the first `Task`). Instead:

1. **Read `.claude/agents/banner-orchestrator.md`** and follow its protocol as your playbook: session init (slug + timestamped folder), phases, all approval gates, single-writer ownership of `session_state.json`, and the abort/backtrack rules.
2. **Dispatch the worker sub-agents via `Task`** as the protocol directs — `brand-researcher` (mode=profile, then mode=logo), `copywriter` (generate, then finalize), `canva-designer` (directions, then backgrounds, then compose). Wait for each JSON envelope, apply its `state_patch`, and persist `session_state.json` yourself.
3. **Own every approval gate via `AskUserQuestion`** (Gates 1, 2, 3, 4a, 4b, 4d) — you are the only one who talks to the user.
4. **Default engine = Nano Banana full-design mode** (`design_mode:"full"`): the logo is generated by Nano Banana (`brand-researcher` Branch B → local PNG), and `canva-designer` renders the COMPLETE banner + header with the Hebrew headline, the logo (`--ref`), and a booking CTA baked in → `resize.js` → `validate_export.js` → `design-qa`. Pass `logo_local_path` to `canva-designer`.

**Final summary (Hebrew):** paths to `final/banner_310x600.png` + `final/header_1366x200.png`, plus any edit URLs.

**On abort/failure:** relay the failing sub-agent's status verbatim to the user.
